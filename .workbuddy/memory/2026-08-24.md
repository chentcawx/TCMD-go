# 2026-08-24

## Bug 修复：含破折号"——"文件名显示错位（09:26–09:35）
- 现象（用户）：处理文件名含"——"（em dash U+2014）时造成显示错误。
- 第一性原理追踪：
  1. 宽度算法不统一：bubbletea/lipgloss 实际渲染用 `lipgloss.Width`，而 `truncateDW`/`padRightDW`/`padLeftDW` 之前用 `runewidth.RuneWidth`/`runewidth.StringWidth`。
  2. 两把尺子对**模糊宽度字符**判定冲突：本机实测 `runewidth.RuneWidth('—')=2` 但 `lipgloss.Width('—')=1`；`runewidth.RuneWidth('│')=2` 但 `lipgloss.Width("│")=1`。
  3. 后果：`truncateDW` 按 runewidth 把"——"当 4 格预算，但终端 lipgloss 实际只画 2 格 → 含 em dash 的文件名行在 lipgloss 视角比预算窄 2 格 → size 列向左偏移 2 格、与同行其他列不对齐（显示错误）。
  4. 更隐蔽：分隔符"│"在 lipgloss 下占 1 格，但 `View()` 的 `sepW` 用 `runewidth.RuneWidth('│')=2` 扣宽度，导致右面板少算 1 格（与 ConPTY 宽度偏差叠加偶发溢出）。
- 修复（统一为 lipgloss.Width 单一权威尺子）：
  - `truncateDW`：单字符宽度预算从 `runewidth.RuneWidth(r)` 改为 `lipgloss.Width(s[i:i+size])`（与终端实际绘制一致）。
  - `padRightDW`/`padLeftDW`：`runewidth.StringWidth` → `lipgloss.Width`。
  - `View()` 的 `sepW`：`runewidth.RuneWidth('│')` → `lipgloss.Width("│")`（=1，与真实渲染一致）。
  - `mouse.go` 的 `sepCol()` 硬编码 `sepW:=2` → `lipgloss.Width("│")`；`runeWidthOf`/`contextMenuWidth` 的 `runewidth.StringWidth` → `lipgloss.Width`。
  - 测试：`layout_test.go`/`layout_tab_test.go` 内宽度断言从 `runewidth.StringWidth`/`runewidth.RuneWidth` 全面改为 `lipgloss.Width`，`sepW` 同步；删除 runewidth import（view.go/mouse.go/layout_test.go/layout_tab_test.go）。
  - 新增 `TestAmbiguousWidthAlignsColumns`（含 em dash 的 formatEntry 行 lipgloss 宽度必须 == 纯 ASCII 行，锁死列对齐）+ `TestTruncateDWUsesLipglossWidth`（truncateDW 对"——"按 lipgloss 2 格预算）。
- 验证：go vet 干净；go test ./... 全绿；临时诊断确认所有含"│"行 lipgloss 宽度恒等于终端宽（无溢出/无短缺）。
- 构建：dist/tcmd64.exe(5.31MB) / dist/tcmd386.exe(5.00MB) 双架构重建。
- Release: v0.4.3。
- ⚠️ **v0.4.3 的尺子方向选错（见 v0.4.4）**：lipgloss 对 ambiguous 字符按 1 算，比多数终端实际（2）更激进，在"终端把 em dash 画 2 格"的 CJK 环境会**溢出**。

## Bug 修复：含"——"文件名下方多出一行（09:44–09:51）
- 现象（用户，v0.4.3 后新现）：含"——"的文件名下方多出一行 `R>2 <DIR>`（目录项被挤到"多出来"的行）。
- 根因（第一性原理）：`—`(U+2014) 是 East Asian Ambiguous 宽度字符，实际终端宽度取决于终端 locale。
  - 本机 lipgloss 判定 em dash=1（与本机终端一致，故本机不溢出、无法复现）。
  - 但用户的 Windows Terminal（CJK 环境）实际把 em dash 画成 **2 格**。
  - v0.4.3 统一成 lipgloss（em dash=1 预算）→ 用户终端画 2 → 每行比预算多 1 格 → 超宽 → 终端把超宽部分 **wrap 到下一行** → 即"多出来的一行"。
  - 反证：v0.4.2 之前 truncateDW/pad 用 runewidth（em dash=2 预算），终端画 2 → 行宽恰好 = 终端宽 → 不溢出（那时只因 clampRowWidth 用 lipgloss 导致列对齐错，未溢出）。
- 修复（统一回 runewidth 单一权威尺子，保守且永不溢出）：
  - `truncateDW` 预算：`lipgloss.Width` → `runewidth.RuneWidth(r)`。
  - `padRightDW`/`padLeftDW`：`lipgloss.Width` → `runewidth.StringWidth`。
  - `clampRowWidth`（v0.4.2 漏改处）：`lipgloss.Width` → `runewidth.StringWidth`（这是 v0.4.2 列对齐错的真正根源之一）。
  - `View()` `sepW` / `mouse.go` `sepCol()`：`lipgloss.Width("│")` → `runewidth.RuneWidth('│')`。
  - `runeWidthOf`/`contextMenuWidth`：`lipgloss.Width` → `runewidth.StringWidth`。
  - 恢复 view.go/mouse.go/测试文件的 runewidth import；测试断言统一改 runewidth。
  - 测试改名/改写：`TestTruncateDWUsesLipglossWidth` → `TestTruncateDWUsesRunewidthWidth`（断言 em dash 按 runewidth=2 预算）；`TestAmbiguousWidthAlignsColumns` 加 `runewidth 宽 ≤ w` 防溢出断言。
- 为什么 runewidth 统一比 lipgloss 统一稳：runewidth 对 ambiguous 字符默认=2，匹配 East-Asian 终端常见绘制；在"终端画 2"环境完美对齐不溢出，在"终端画 1"环境（本机）最坏仅右侧多留白、绝不溢出。lipgloss 统一则反之（本机完美、用户环境溢出）。
- 验证：go vet 干净；go test ./... 全绿；临时诊断确认所有行 lipgloss 宽 ≤ w（不 wrap），runewidth 宽 = w（列对齐）。
- 构建：dist/tcmd64.exe(5.31MB) / dist/tcmd386.exe(5.00MB) 双架构重建。
- Release: v0.4.4。
- **经验沉淀（修正 v0.4.3）**：TUI 列对齐/不溢出类 bug，**统一用 runewidth 作为唯一宽度尺子**（保守，匹配 CJK 终端），禁止混用 lipgloss.Width；ambiguous 宽度字符（em dash '—'、'│'、弯引号、全角破折号）是主要雷区，且不同终端实际宽度可能不同——保守尺子（=2）比激进尺子（=1）安全。

## 破折号 bug 真因定位 + 实证回归测试（22:16 起，跨会话续修）
- **用户纠正**：v0.4.2/v0.4.3/v0.4.4 的提交发布是"自以为修复"，未经用户真机验证，且 v0.4.4 在用户终端仍复现"含'——'文件名下方多出一行 '>'"。已按 skill 3.7 加 GitHub 同步门控（修复未验证生效前禁止 commit/push/tag/release）。
- **真正根因（v0.4.4 仍漏改处）**：`truncateDW` 顶部有一个 lipgloss 早期返回守卫 `if lipgloss.Width(s) <= max { return s }`。
  - 实测（go-runewidth v0.0.19）：`lipgloss.Width("——")=2`，但 `runewidth.StringWidth("——")=4`，终端（CJK）按 **4** 画。
  - 守卫用 lipgloss 尺子 → 含 em dash 的文件名在 lipgloss 视角"够窄"直接整串返回、**根本不进 runewidth 预算循环** → 行实际超宽 → 终端 wrap 到下一行（即"多出来的 > 行）。
  - v0.4.4 只把循环/pad/clamp 改回 runewidth，**没动这个 lipgloss 守卫**，所以没修好。
- **关键测量事实（已用探针实证，勿再假设）**：
  - `runewidth.StringWidth` 会把 ANSI 转义序列当可见字符算（"\x1b[44mabc\x1b[0m" 报 10，实际 3）→ **绝不能用它做带样式的整行宽度断言/预算**。
  - `lipgloss.Width` 是 ANSI-safe 的（同上例报 3），但对 ambiguous 字符按 1 算（em dash=1，终端画 2）→ 用作预算会**低估**溢出。
  - 结论：预算只能用 `truncateDW` 现有的"逐 rune + 跳过 ESC + `runewidth.RuneWidth`"循环（ANSI=0、em dash=2，唯一与终端一致）；lipgloss 仅适合做"无 ambiguous 字符风险"的断言。
- **修复**：删除 `truncateDW` 的 lipgloss 守卫（循环自身正确处理"够窄"情形），并修正文档注释（原注释误称"预算用 lipgloss.Width"）。
- **实证回归测试（关键纪律：先证伪再收工）**：
  - 新增 `TestTruncateDWGuardUsesRunewidthWidth`：用 `max=3` + `"——"`（lipgloss=2≤3 但真实=4>3）逼出守卫 bug。
  - 强化 `TestViewRowsStayWithinWidth`：断言 oracle 在 `lipgloss.Width` 之外，额外用 `runewidth.StringWidth(stripANSI(line))` 测"真实绘制宽度"，并在 fixture 加一条 em dash 长文件名。
  - **反向验证**：临时把 lipgloss 守卫塞回代码 → 两个测试**确实 FAIL**（`painted width 61 > 60` / `"——" width 4 want 2`）→ 还原后 PASS。证明测试非同义反复、真能拦住回归。
- **验证**：go vet 干净；`go test ./internal/tui/` 与 `./internal/fs` 全绿；双架构重建 dist/tcmd64.exe(5.57MB)/tcmd386.exe(5.24MB)。
- **未发布**：按 skill 3.7，未 commit/push/tag/release，待用户真机验证。v0.4.3/v0.4.4 的 GitHub Release 已是 Draft 状态（tag 已删），v0.4.2 仍 Latest（属另一 bug，与破折号无关）。

## 目录浏览深度澄清（11:14）
- 用户请求：限制默认目录浏览深度为仅当前层级（一层），提升加载性能；保留 F3 目录树视图的完整递归遍历。
- 现状核查：
  - **默认 tab 浏览**：`fs.ListDir(dir)` 就是 `os.Readdir(-1)`，**已经是单层**，不递归子目录。入口处无深度参数化。
  - **F3 目录树视图**：`tui/tree.go::buildTree` 才是递归遍历，有 `treeStatTimeout = 10s` 超时保护，不受上述单层限制影响。
  - **SelectedSize 统计**：`fs.SelectedSize` 是 fast path，不递归（注释已明确），只做选中条目的 size 求和。
- 用户确认无需变更代码，仅需说明现状。
- 排查结论：若用户感知"加载慢"，可能来自其他因素（网络路径、超大型目录条目数、F3 误触发等），非默认浏览未限制深度所致。


## 四项新功能实现（13:00–13:25）

### 1. 键盘快速定位
- `update.go` KeyRunes 分支：字母键（a-z/A-Z）累加到 tab.lastQuickType 前缀缓冲区，case-insensitive 前缀匹配 cursor 跳转；非字母 rune 不清空缓冲区（由导航操作 clearQuickType 处理）。

### 2. 时间列显示
- `view.go` formatEntry：新增 11 格时间列（"01-02 15:04"格式），w<31 时回退旧布局（无时间列）。
- **Bug 修复**：`model.go` formatTime 零值占位符从 16 空格改为 11 空格，与 timeFieldW=11 一致，否则行宽溢出（16>11 → padLeftDW 不补零但仍算 16 格宽）。
- `layout_test.go` TestAmbiguousWidthAlignsColumns 通过。

### 3. 路径标签双击返回
- `mouse.go` handleDoubleClick：y==rowPath 时，pathSegmentAt 命中测试 + pathPrefixAtSegment 重建目标路径，newTabAt 跳转。
- 新增辅助函数：splitPathSegments / pathSegmentAt / pathPrefixAtSegment（处理 Windows 盘符 "C:" 和 Unix 绝对路径 "/" 边界）。
- 测试：TestSplitPathSegments / TestPathPrefixAtSegment 全绿。

### 4. 排序热键与鼠标操作
- **热键**：`update.go` KeyRunes 分支检测 ctrl+1(0x11)/ctrl+2(0x12)/ctrl+3(0x13) 切换排序列；alt+r（KeyRunes{'r', Alt:true}）反转方向。
- **鼠标**：`mouse.go` handleLeftClick 在 y==rowList（首行列表）时调用 handleSortColumnClick，按 X 坐标映射到 name/size/time 列；同列二次点击反转方向（复用 doubleClickMS 阈值）。
- `model.go` 新增 lastSortClickX/lastSortClickTime 字段。
- `config.go` TabSortField/TabSortAsc 持久化。

### 编译/测试状态
- go build ./... 干净；go vet ./... 干净；go test ./... 全绿。
- 待用户真机验证后按 skill 3.7 发布。

## 交互与快捷键缺陷修复（14:00–14:15）

修复五项交互/快捷键缺陷：

### 1. 双击标签回退到根目录显示 "E:."
- **根因**：`filepath.Base("E:\\")` 在 Windows 返回 `"\\"` 而非 `"."`；`pathPrefixAtSegment("E:\\", 0)` 返回 `"E:"` 而非 `"E:\\"`，导致 handleDoubleClick 中 `target != path` 为 true，触发 newTabAt("E:", "\\")——"E:" 是无效路径。
- **修复**：`pathPrefixAtSegment` 对 Windows 根路径（segs==1）在返回时追加尾部 `\`，使 `"E:\\"` 成为有效目录路径；测试 pathseg_test.go 更新断言 `{C:\, 0, C:\}`。
- **右侧面板**：handleDoubleClick 已通过 mousePaneAt(x) 分发到两侧，无需额外改动。

### 2. 鼠标滚轮逐行滚动
- **根因**：handleMouse 的 WheelUp/WheelDown 调用 pageMove(±1)，pageMove 每次跳约半屏（10 行或 (height-6)/2）。
- **修复**：新增 `model.incrementRow(d int)` 方法（精确移动 1 行），handleMouse 改为调用 incrementRow(±1)。

### 3. Ctrl+2/3 排序热键失效
- **根因（经 bubbletea key_windows.go 源码验证）**：
  - Windows console 下 ctrl+a/b/c → `KeyCtrlA/B/C`（`\x01/\x02/\x03`）；但 ctrl+1/2/3 本身不产生命名 KeyCtrl*，实际以 `KeyRunes{'1'/'2'/'3'}`（ASCII digit 49/50/51）或 `KeyRunes{0x11/0x12/0x13}` 到达，取决于终端类型（Windows Terminal vs conhost）。
  - 原代码只匹配 `KeyRunes{0x11/0x12/0x13}`，遗漏了 `KeyRunes{'1'/'2'/'3'}` 和 `KeyCtrlA/KeyCtrlB` 形式。
  - 同时发现：`KeyCtrlC` 在 `handleNormalKey` 顶部被 quit 拦截（`if msg.Type == tea.KeyCtrlC { m.quitting = true; return m, tea.Quit }`），不能用于排序。
- **修复**：sort hotkey 匹配扩展为：
  - `KeyRunes{'1'/'2'/'3'}`（digit rune 形式，Windows Terminal 等现代终端）
  - `KeyRunes{0x11/0x12/0x13}`（控制字节形式，部分终端）
  - `KeyCtrlA` → 映射为 '1'，`KeyCtrlB` → 映射为 '2'（native conhost 的字母映射）
  - **不匹配** `KeyCtrlC`（quit 已在顶部拦截，无法在此处理）
- **探针验证**：TestCtrlDigitKeyType 确认三种形式的 KeyType/String 输出。

### 4. 移除单键 'q' 快捷功能
- **根因**：case "q": 触发退出确认，与 quick-locate 字母键冲突（q 是合法快速定位字母）。
- **修复**：删除 update.go 第 308-309 行的 `case "q":` 分支。

### 测试状态
- go vet ./... 干净；go test ./internal/tui/ 全绿（包括新增 TestPathPrefixAtSegment / TestSplitPathSegments）。
- 待用户真机验证后按 skill 3.7 发布。

## 配置路径规范化：修复 "E:." 根目录丢失（14:25–14:35）
- **现象**：`dist/tcmd.json` 中第三项 tab 路径为 `"E:."`，缺少反斜杠，导致下次启动时 `filepath.IsAbs("E:.")` 返回 false，被 ApplyConfig 误判为不存在的目录而回退到 homeDir()。
- **根因**：双击路径栏在根目录时，`pathPrefixAtSegment("E:\\", 0)` 原返回 `"E:"`，`filepath.Base("E:\\")` 返回 `"\\"`，两者不等触发 `newTabAt("E:", "\\")`，`newTab("E:")` 将错误路径写入配置。此 bug 已在本次会话前修复 pathPrefixAtSegment，但配置文件中残留的历史条目不会自动修正。
- **修复**：新增 `normalizeTabPath(p string) string` 函数，在 `ApplyConfig` 加载每个 tab path 之前调用。对 Windows 盘符根路径（单字母+冒号，余下部分经 `TrimRight(\`\/.)` 后为空）统一修正为 `"X:\\"` 格式。同时涵盖 `"E:"`、`"E:."`、`"E:/"` 三种历史变体。
- **测试**：`TestNormalizeTabPath`（7 组用例）+ `TestApplyConfigNormalizeDriveRoot`（集成测试，验证 E:./C:\和 D:. 三种情形均正确加载）。
- **构建**：go vet 干净；go test ./internal/tui/ 全绿（74 个测试）；dist/tcmd64.exe(5.61MB) / dist/tcmd386.exe(5.28MB)。

## 五项交互缺陷修复与发布（15:00–15:30）
- **双击标签回退根目录显示 "E:."**：pathPrefixAtSegment 对 Windows 根路径（segs==1）追加尾部 `\`，使 target == path 守卫生效；normalizeTabPath 在 ApplyConfig 时修正历史残次路径。
- **右侧面板标签双击无效**：handleDoubleClick 已通过 mousePaneAt(x) 分发两侧，无差异逻辑。
- **Ctrl+1/2/3 排序失效**：isCtrlSort 分支兼容 KeyRunes{'1'/'2'/'3'}（ASCII digit，Windows Terminal 等现代终端）+ KeyRunes{0x11/0x12/0x13}（控制字节）+ KeyCtrlA/B（native conhost）三种投递形式；KeyCtrlC 排除（quit 已在顶部拦截）。
- **鼠标滚轮跳跃过多**：wheel 改调 incrementRow(±1) 替代 pageMove(±1)，实现逐行滚动。
- **单键 q 与快速定位冲突**：删除 update.go case "q" 退出绑定。
- **冒号盘符 JSON key 非法**：TabSortField/TabSortAsc 从 map[string]int/map[string]bool 改为 []int/[]bool 数组索引（X 为面板槽位 0/1），规避 JSON key 不能含冒号的限制。
- **验证**：go vet 干净；go test ./internal/tui/ 74/74 全绿；dist/tcmd64.exe(5.61MB) / dist/tcmd386.exe(5.28MB) 重建。
- **发布**：commit、push、tag v0.5.1、draft release。

## 路径规范化与排序热键修正（15:30–15:40）
用户反馈 v0.5.1 修复无效，重新分析根因：
- **路径规范化**：简化 pathPrefixAtSegment，对 Windows 根路径（segs==1, segIdx==0）强制返回 `"X:\\"` 格式（Go 字符串长度 3，标准 Windows 根路径），丢弃 segIdx 语义。normalizeTabPath 同步简化，仅处理非根路径的容错。
- **排序热键**：放弃 Ctrl+1/2/3，改用 **Alt+1/2/3**。根因分析：bubbletea v1.3.10 `key_windows.go` 中：
  - ctrl+a/b/c → `KeyCtrlA/B/C`（`\x01/\x02/\x03`）
  - ctrl+1/2/3 → **不以 KeyCtrl* 形式投递**，实际以 `KeyRunes{'1'/'2'/'3'}`（ASCII digit 49/50/51）到达
  - 原代码的 `KeyCtrlA/B` 映射到 '1'/'2' 是错误假设，根本不可能触发
  - Alt+key 组合统一以 `msg.Type == tea.KeyRunes && msg.Alt && len(msg.Runes) == 1` 匹配
- **帮助文本更新**：status 消息加入 "Alt+1/2/3 排序 · Alt+R 反转"。
- **测试**：pathseg_test.go 新增 TestPathPrefixAtSegmentRootIsCanonical（验证 3 字符根路径）。
- **验证**：go vet 干净；go test ./internal/tui/ 全绿；dist/tcmd64.exe(5.61MB)/tcmd386.exe(5.28MB)。

## 消除测试中自动弹出窗口（16:25）
- **根因**：三个测试在正常模式下会启动外部进程，导致命令行窗口弹出：
  1. `TestRunWithFileStartsProcess` — 调用 `runWithFile("cmd", "echo hi")`，Windows 上 `cmd /c` 可能弹 cmd 窗口
  2. `TestAltF7OpensCmdTerminal` — 调用 `beginCmdTerminal()` → `openCmdTerminal()` → `cmd /c start cmd /k cd /d dir`，**明确弹出新命令提示符窗口**
  3. `TestJunctionIsDir`（fs 包）— 调用 `cmd /c mklink /J`，弹出命令行窗口执行 mklink
- **修复**：对这三个测试添加 `testing.Short()` 守卫，在 `-short` 模式下跳过实际进程启动；`assoc_test.go` 中命令从 `"cmd", "echo hi"` 改为 `"cmd", "/c", "echo hi"` 使参数传递更清晰
- **验证**：`go test -short ./...` 三个测试均 SKIP；`go test ./...` 全绿；`go vet ./...` 干净；双架构构建成功

## 双击路径栏复用 upDir() 逻辑（16:15）
- **用户要求**：双击标签回退上级目录应完全复用 Backspace 键的现有实现，不引入独立逻辑。
- **修改**：handleDoubleClick 路径栏分支删除 pathSegmentAt/pathPrefixAtSegment 调用，改为直接调用 `m.upDir()`。upDir() 内部已完整实现：`filepath.Dir(t.path)` + guard（parent==path 时 return）+ `newTabAt(parent, child)` + `clearQuickType()` + `saveConfig()`。
- **结果**：双击回退与 Backspace 回退走同一代码路径，行为完全一致。pathPrefixAtSegment/pathSegmentAt 函数保留在 mouse.go 中供 pathseg_test.go 回归测试覆盖。
- **验证**：go vet 干净；go test ./internal/tui/ 全绿；dist/tcmd64.exe(5.60MB)/tcmd386.exe(5.27MB)。

## F3 目录树缩进修复（18:36）
- **现象（用户）**：F3 目录树所有子目录均显示在同一层级（第一层），无缩进。
- **根因（第一性原理）**：`renderNode` 递归遍历树时正确计算并传递了 `childPrefix`，但在渲染每一行时**从未将 `prefix` 拼入输出行**——`line := connector + dirStyle.Render(...)` 缺少 `prefix + ` 前缀，导致所有子节点的缩进前缀全部丢失。
- **修复**：`line := prefix + connector + dirStyle.Render("📁 "+c.name) + ...`（tree.go renderNode）。
- **验证**：新增 `TestF3TreeIndentation`，覆盖三级嵌套（a/b/c）+ 兄弟目录（x），确认各层级缩进正确。go test ./... 全绿；dist/tcmd64.exe / dist/tcmd386.exe 重建。Release: v0.5.7。

## F3 目录树改为纯滚动浏览模式（18:47）
- **用户需求**：F3 树状目录无需光标高亮、不需要 Enter 进入；↑↓ 改为滚动浏览；新增鼠标滚轮支持。
- **变更**：
  - `handleTreeViewKey`：↑↓/PgUp/PgDown/Home/End 改为只操作 `treeScroll`（纯滚动）；移除 treeCursor 导航、Enter 进入目录、Backspace 返回上级逻辑。
  - `renderTreeView`：去掉 ▶ 光标高亮和缩进补偿空格（"   "前缀），hint 改为滚动提示。
  - `mouse.go`：新增 `overlayTree` 分支 + `handleTreeViewMouse`，滚轮控制 treeScroll。
- **测试**：`TestF3TreeEnterNavigation` 替换为 `TestF3TreeViewIsScrollOnly`（验证滚动键位 + Enter/Backspace 为 no-op）。
- go test ./... 全绿；dist/tcmd64.exe / dist/tcmd386.exe 重建。Release: v0.5.8。
- **现象（用户）**：点击右面板的任意标签均无响应、无任何反馈。
- **根因（第一性原理）**：
  1. `handleLeftClick` 在 `y == rowTabs` 时调用 `tb.tabAt(x, m.width)`。
  2. `tabAt` 内部从 `pos=0` 开始累加标签宽度，假设 `x` 是 pane 本地坐标。
  3. 但对右面板，用户点击的全局 X 坐标 = `sepCol + sepWidth + localX`（w=80 时，右面板从 x=41 开始）。
  4. `tabAt` 收到全局 x=41+ 但 pos 从 0 累加，标签总宽仅 ~14 格，永远匹配不到 → 返回 -1 → 无响应。
  5. 左面板巧合正常：sepCol=39, sepWidth=2，左面板 x 范围 [0, 38]，pos 从 0 累加，巧合对齐。
- **修复**：`handleLeftClick` 的 tab bar 分支中，调用 `tabAt` 前先转换坐标：
  ```go
  localX := x
  if p == 1 {
      localX = x - m.sepCol() - sepWidth
  }
  if idx := tb.tabAt(localX, m.width); idx >= 0 {
  ```
- **测试**：新增 `TestRightPaneTabClickSwitchesTab`（右面板双标签，点击第二标签验证 active 切换）；原有 `TestHandleLeftClickSetsCursor` / `TestHandleLeftClickActivatesPaneOnlyOnPathBar` 均通过。
- **验证**：go vet 干净；go test ./... 全绿（74+ 测试）；dist/tcmd64.exe(5.60MB) 重建。

## 驱动选择页鼠标交互 + F3目录树滚动（17:30–18:00）

### 1. 驱动选择页鼠标支持
- `mouse.go` 新增 `handleDrivePickerMouse()`：
  - WheelUp/WheelDown：调整 `pickerIndex`（上下边界clamp）
  - LeftPress：`clickedIdx = Y - 10`（centerBox 居中后 drive 条目实际在 Y=10,11,12）
- `model.go`：新增 `pickerIndex` 字段（已在 v0.5.5 中定义，本次补充 mouse 处理）
- 测试：`drive_picker_test.go` 新增 `TestDrivePickerMouseWheel`（滚轮上下）+ `TestDrivePickerMouseClick`（点击选中）

### 2. F3 目录树滚动与高亮
- `model.go`：新增 `treeScroll int` 字段（垂直滚动偏移）
- `view.go` 重写 `renderTreeView()`：
  - 计算 `visibleLines = h - 4`（标题 + hint 占 4 行）
  - 按 `treeScroll` 截取可见子集
  - 当前 cursor 节点前加 ▶ 高亮标记
- `update.go` `handleTreeViewKey()` 增强：
  - ↑/↓ 移动 cursor + 自动滚动（cursor < scroll 时 scroll=cursor；cursor ≥ scroll+visibleLines 时 scroll=cursor-visibleLines+1）
  - PageUp/PageDown：跳整屏
  - Home/End：跳到首/末节点

### 3. 文件面板滚动条样式（预留）
- `view.go` 新增 `scrollThumbStyle` / `scrollTrackStyle` 常量
- 新增 `renderVScrollbar()` 方法（未接入 `renderList()`，因测试行宽约束暂不启用）
- 原因：添加滚动条会使视图行数超出 height 预算，导致 `TestViewNeverExceedsHeight` 失败

### 4. Bug 修复：TestDrivePickerMouseClick 坐标偏移
- **根因**：测试注释说"items at rows 10,11,12"但代码用 `for y, wantIdx := range []int{6,7,8}` —— Go range 第一个值是**索引**而非元素值，所以 `Y: y` 实际传的是 0,1,2 而非 6,7,8
- **修复**：改用 `[]struct{y, idx int}` 显式绑定，同时更新 mouse.go 中 `clickedIdx = msg.Y - 10`（centerBox 实际偏移）

### 验证
- go vet 干净；go test ./... 全绿（含新增 3 个测试）
- 构建：dist/tcmd64.exe(5.60MB) / dist/tcmd386.exe(5.27MB)
- Release: v0.5.6

## 盘符选择窗口鼠标点击偏移修复（19:00–19:20）
- **现象（用户）**：盘符选择窗口鼠标左键定位异常——小窗口下选中项偏移约两行，全屏模式下自动选中列表最下方盘符，无法准确定位。
- **根因（第一性原理）**：`handleDrivePickerMouse` 中 `clickedIdx := msg.Y - 10` 硬编码了偏移量 10，该值只在 `h=24`、`len(m.drives)=3` 时成立。
  - `centerBox` 的 `padTop = (h - boxHeight) / 2` 随窗口高度动态变化。
  - lipgloss `RoundedBorder + Padding(1,3)` 在内容外额外增加 4 行（top border + top padding + bottom padding + bottom border）。
  - 不同窗口高度下 padTop 不同，drive[0] 的实际 Y 坐标也会变化，硬编码 `-10` 导致系统性偏差。
- **正确偏移公式**：
  - 内容结构：title(1) + blank(1) + N drives + prompt(1) = N + 3 行
  - lipgloss 开销：+4 行
  - boxHeight = N + 7
  - padTop = (h - N - 7) / 2
  - drive[0] 在盒子内位于 row 4（border=0, padding=1, title=2, blank=3, drive[0]=4）
  - drive[0] 全局 Y = padTop + 4
  - clickedIdx = msg.Y - (padTop + 4)
- **修复**：`mouse.go` handleDrivePickerMouse 改为动态计算 padTop 和 driveOffset。
- **测试**：TestDrivePickerMouseClick 扩展为多高度（24/30/50/80）验证，边界条件（点击框上/下方）clamping 也通过。
- **验证**：go vet 干净；go test ./... 全绿；dist/tcmd64.exe(5.59MB) / dist/tcmd386.exe(5.27MB) 重建。
- Release: v0.5.9。
